# 📘 데이터 중심 애플리케이션 설계

## Chapter 4. 부호화와 발전

---


Chapter 4를 읽으면서 가장 중요하게 느낀 점은 “데이터 부호화 방식은 단순한 포맷 선택 문제가 아니라, 시스템이 변화에 얼마나 잘 버티느냐의 문제”라는 점이었다.
특히 실무에서 자주 사용하는 **RESTful API 환경**에서는 서버와 클라이언트가 동시에 업데이트되지 않고 서로 다른 버전의 코드와 데이터가 공존하는 상황이 매우 흔하다
상위·하위 호환성 문제를 이해하고 나니, REST API에서 필드 하나를 추가하거나 바꾸는 일도 단순한 구현이 아니라 **시스템 진화를 결정하는 설계 행위**라는 점을 다시 보게 되었다.

---

## 2. REST API 관점에서 본 호환성 정리

### 2.1 하위 호환성 (Backward Compatibility)

정의

- **서버의 새로운 코드가, 예전 버전의 클라이언트 요청/데이터를 문제없이 처리할 수 있는 것을 의미**

REST API에서의 상황

- 서버 API를 먼저 배포한다
- 클라이언트나 DB에는 예전 형식의 데이터가 남아 있다
- 새 서버는 예전 요청과 데이터를 그대로 처리해야 한다

중요한 이유

- 서버는 점진적 배포가 가능하지만
- 이미 저장된 데이터와 기존 요청 형식은 즉시 변경할 수 없다

핵심 포인트

- 기존 필드를 쉽게 제거하지 않는다
- 누락된 값에 대비해 기본값 처리가 중요하다

---

### 2.2 상위 호환성 (Forward Compatibility)

정의

- **예전 버전의 클라이언트가, 새로 변경된 API 응답을 받아도 정상 동작하는 것을 의미**

REST API에서의 상황

- 서버 응답 포맷이 변경되었다
- 모바일 앱이나 외부 클라이언트는 여전히 예전 버전을 사용 중이다
- 새 응답을 받아도 클라이언트가 깨지지 않아야 한다

중요한 이유

- 클라이언트는 서버가 통제할 수 없다
- 모바일 앱과 외부 API 연동에서 장애로 바로 이어질 수 있다

핵심 포인트

- 알 수 없는 필드는 무시 가능해야 한다
- 필드 이름 변경과 타입 변경은 매우 위험하다

---

## 3. RESTful API에서의 데이터 흐름 관점 정리

REST API를 기준으로 보면 데이터 흐름은 다음과 같다.

1. 서버 내부 객체 
   - 도메인 모델, 엔티티, DTO 등
   - 아직은 순수한 언어(Java, Kotlin, JS 등) 객체 상태

2. 부호화 (Encoding)
   - 서버 객체를 JSON 같은 포맷으로 변환
   - 필드 이름, 타입, 구조가 **이 시점에 확정**됨

3. HTTP 응답 바디
   - 문자열(JSON) 또는 바이트 배열
   - 이미 포맷이 고정된 상태
   
4. 네트워크 전송
   - HTTP를 통해 그대로 전달
   - TCP/IP는 데이터 의미를 전혀 모름

5. 클라이언트에서 복호화 (Decoding)
   - JSON을 파싱해서 객체로 변환
   - 서버가 어떤 필드를 보냈는지 여기서 해석

6. 클라이언트 내부 객체
   - ViewModel, State, DTO 등
   - 실제 화면 로직에 사용

여기서 중요한 점은

**호환성 문제는 네트워크 구간이 아니라, 2번과 5번 지점에서 발생한다는 것**이다.

---

## 4. REST API에서 부호화와 복호화가 깨지는 지점

### 4.1 서버 → 클라이언트 (부호화 관점)

서버가 JSON 응답을 만들 때 발생할 수 있는 문제

- 필드 추가
- 필드 삭제
- 필드 이름 변경
- 필드 타입 변경

이 중에서 상대적으로 안전한 변경

- 새로운 필드 추가

위험한 변경

- 필드 삭제
- 필드 이름 변경
- 숫자 타입 변경 (int → long, number → string 등)

---

### 4.2 클라이언트 → 서버 (복호화 관점)

클라이언트 요청 JSON을 서버가 파싱할 때 발생할 수 있는 문제

- 클라이언트가 보내던 필드를 더 이상 보내지 않음
- 새로운 필드를 추가해서 보냄
- 타입이 바뀐 값을 보냄

서버 입장에서의 안전한 전략

- 알 수 없는 필드는 무시
- 필수 값은 명확히 검증
- optional 필드는 기본값 처리

---

## 5. JSON 기반 REST API와 스키마 진화의 현실

Chapter 4에서 Thrift, Protobuf, Avro는

**스키마 진화를 공식적으로 지원하는 도구**로 소개된다.

하지만 REST API에서 JSON을 쓰는 경우 대부분은

- 명시적인 스키마 파일이 없다
- 문서(OpenAPI, Swagger)로만 스키마를 공유한다
- 실제 호환성은 개발자 규칙과 관례에 의존한다

그래서 JSON API에서는 다음 규칙이 사실상의 스키마 진화 전략이 된다.

---

## 6. REST API에서 실무적으로 지켜야 할 호환성 규칙

### 6.1 필드 추가 규칙

- 항상 optional로 추가한다
- 기존 필드 의미를 변경하지 않는다
- 기본 동작이 바뀌지 않도록 한다

### 6.2 필드 삭제 규칙

- 즉시 삭제하지 않는다
- Deprecated 상태로 충분히 유지한다
- 클라이언트 업데이트 완료 후 제거한다

### 6.3 필드 이름 변경 규칙

- 기존 필드는 유지
- 새로운 필드를 추가
- 일정 기간 두 필드를 함께 제공

### 6.4 타입 변경 규칙

- 가능한 한 피한다
- 불가피하다면 새로운 필드로 분리한다



